# Examples

Place your input multi-view images in `examples/input/` (JPG or PNG), then run inference as described in the [project README](../README.md).

Reconstructed point clouds are written to `examples/output/`.

## Expected layout

```
examples/
├── input/      # 2-8 multi-view images of the same scene
└── output/     # generated .ply files
```
